name: Actualizar datos de cartas

on:
  workflow_dispatch:
    inputs:
      format:
        description: "Formato a scrapear (PE, PB, SB, FX, NE) — vacío = todos"
        required: false
        default: ""
      limit:
        description: "Límite de ediciones (vacío = sin límite, útil para probar)"
        required: false
        default: ""
  schedule:
    # Cada lunes a las 06:00 UTC actualiza el catálogo
    - cron: "0 6 * * 1"

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - name: Configurar Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Ejecutar scraper
        working-directory: scraper
        run: |
          ARGS=""
          if [ -n "${{ github.event.inputs.format }}" ]; then ARGS="$ARGS --format ${{ github.event.inputs.format }}"; fi
          if [ -n "${{ github.event.inputs.limit }}" ]; then ARGS="$ARGS --limit ${{ github.event.inputs.limit }}"; fi
          echo "node scrape.js $ARGS"
          node scrape.js $ARGS

      - name: Actualizar ban list (Primera Era — Racial Edición)
        # No depende de --format/--limit: es una sola página, siempre completa.
        # No debe tumbar el workflow si blog.myl.cl cambia de formato — solo se
        # queda con el data/banlist.json anterior y lo avisa en el log.
        working-directory: scraper
        run: node scrape-banlist.js || echo "⚠ Scraper de ban list falló, se mantiene data/banlist.json anterior"

      - name: Subir artefacto (para revisión)
        uses: actions/upload-artifact@v4
        with:
          name: cards-json
          path: |
            data/cards.json
            data/banlist.json

      - name: Commit de los datos actualizados
        # En ejecuciones de prueba (con 'limit') no se hace commit, solo el artefacto.
        if: ${{ github.event.inputs.limit == '' }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if git diff --quiet -- data/cards.json data/banlist.json; then
            echo "Sin cambios en data/cards.json ni data/banlist.json"
          else
            git add data/cards.json data/banlist.json
            MSG="datos: actualiza catálogo de cartas desde tor.myl.cl"
            if ! git diff --cached --quiet -- data/banlist.json; then
              MSG="$MSG + ban list Racial Edición desde blog.myl.cl"
            fi
            git commit -m "$MSG"
            # Reintenta integrando cambios remotos (evita rechazo si la rama avanzó)
            for i in 1 2 3; do
              git push origin ${{ github.ref_name }} && break
              echo "push rechazado, integrando cambios remotos (intento $i)…"
              git pull --rebase -X ours origin ${{ github.ref_name }} || git rebase --abort
            done
          fi
